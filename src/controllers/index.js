const userController = require('./loginController');
const categoryController = require('./categoryController');
const postController = require('./postController');

module.exports = {
    userController,
    categoryController,
    postController,
};